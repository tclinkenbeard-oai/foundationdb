[[test]]
testTitle = 'AutomaticIdempotency'

    [[test.workload]]
    testName = 'AutomaticIdempotencyCorrectness'
    minMinAgeSeconds = 15
    pollingInterval = 5.0
    disableAutomaticIdempotencyProbability = 0.0

    [[test.workload]]
    testName='Attrition'
    testDuration=10.0
